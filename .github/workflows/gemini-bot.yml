# Rozbitý Gemini bot vypnutý (issue #35–#37). Zapni ručně, až bude workflow celé.
name: Gemini bot (vypnuto)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Gemini bot je vypnutý."
